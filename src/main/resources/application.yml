spring:
  application:
    name: test

  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    #
jwt:
  secret : siuiamronaldo